// Generated by the GenAI Jenkinsfile plugin — global reference stub (read-only).
// The standard Jenkins declarative syntax; powers completion, docs and navigation.
// Format: a /** */ doc block (with @kind and optional @param) above each `def`.

/**
 * Check if a GitLab merge request has a specific label
 * @kind step
 */
def GitLabMergeRequestLabelExists() {}

/**
 * Post condition: run if the run was aborted (e.g. manually cancelled).
 * @kind post
 * @allowedIn post
 * @unique
 */
def aborted() {}

/**
 * Accept GitLab Merge Request
 * @kind step
 */
def acceptGitLabMR() {}

/**
 * Add comment on GitLab Merge Request
 * @kind step
 */
def addGitLabMRComment() {}

/**
 * Where the pipeline (or a stage) runs. e.g. <code>agent any</code>, <code>agent none</code>, <code>agent { label 'linux' }</code>, <code>agent { docker 'maven:3.9' }</code>.
 * @kind directive
 * @allowedIn pipeline, stage
 * @unique
 */
def agent() {}

/**
 * When condition: all nested conditions must be true.
 * @kind when
 * @allowedIn when, allOf, anyOf, not
 */
def allOf() {}

/**
 * Post condition: run regardless of outcome.
 * @kind post
 * @allowedIn post
 * @unique
 */
def always() {}

/**
 * Color ANSI Console Output
 * @kind step
 * @param colorMapName
 * @param body
 */
def ansiColor(colorMapName,body) {}

/**
 * Run on any available agent.
 * @kind agent
 */
def any() {}

/**
 * When condition: any nested condition true.
 * @kind when
 * @allowedIn when, allOf, anyOf, not
 */
def anyOf() {}

/**
 * Advanced/Deprecated Archive artifacts
 * @kind step
 * @param includes
 */
def archive(includes) {}

/**
 * Windows Batch Script
 * @kind step
 * @param script
 */
def bat(script) {}

/**
 * When condition: stage runs only for matching branch(es).
 * @kind when
 * @allowedIn when, allOf, anyOf, not
 */
def branch() {}

/**
 * Build a job
 * @kind step
 * @param job
 */
def build(job) {}

/**
 * Catch error and set build result to failure
 * @kind step
 * @param body
 */
def catchError(body) {}

/**
 * Post condition: run if the current status differs from the previous run.
 * @kind post
 * @allowedIn post
 * @unique
 */
def changed() {}

/**
 * Check out from version control
 * @kind step
 * @param scm
 */
def checkout(scm) {}

/**
 * Post condition: always runs last, after the others.
 * @kind post
 * @allowedIn post
 * @unique
 */
def cleanup() {}

/**
 * Run build steps in a container
 * @kind step
 * @param name
 * @param body
 */
def container(name,body) {}

/**
 * Advanced/Deprecated Get container log from Kubernetes
 * @kind step
 * @param name
 */
def containerLog(name) {}

/**
 * Recursively delete the current directory from the workspace
 * @kind step
 */
def deleteDir() {}

/**
 * Change current directory
 * @kind step
 * @param path
 * @param body
 */
def dir(path,body) {}

/**
 * Run inside a Docker image.
 * @kind agent
 */
def docker() {}

/**
 * Record trace of a Docker image used in FROM
 * @kind step
 */
def dockerFingerprintFrom() {}

/**
 * Advanced/Deprecated Record trace of a Docker image run in a container
 * @kind step
 * @param containerId
 */
def dockerFingerprintRun(containerId) {}

/**
 * Print Message
 * @kind step
 * @param message
 */
def echo(message) {}

/**
 * Fetches the environment variables for a given tool in a list of 'FOO=bar' strings suitable for the withEnv step.
 * @kind step
 */
def envVarsForTool() {}

/**
 * Key/value environment variables, available as <code>$NAME</code>. Supports <code>credentials('id')</code>.
 * @kind directive
 * @allowedIn pipeline, stage, when, allOf, anyOf, not
 * @unique
 */
def environment() {}

/**
 * Error signal
 * @kind step
 * @param message
 */
def error(message) {}

/**
 * When condition: stage runs if the Groovy expression is true.
 * @kind when
 * @allowedIn when, allOf, anyOf, not
 */
def expression() {}

/**
 * Post condition: run only if the build failed.
 * @kind post
 * @allowedIn post
 * @unique
 */
def failure() {}

/**
 * Verify if file exists in workspace
 * @kind step
 * @param file
 */
def fileExists(file) {}

/**
 * Post condition: run if the previous run failed/unstable and this one succeeded.
 * @kind post
 * @allowedIn post
 * @unique
 */
def fixed() {}

/**
 * Advanced/Deprecated Get contextual object from internal APIs
 * @kind step
 * @param type
 */
def getContext(type) {}

/**
 * Git
 * @kind step
 * @param url
 */
def git(url) {}

/**
 * Notify gitlab about pending builds
 * @kind step
 * @param body
 */
def gitlabBuilds(body) {}

/**
 * Update the commit status in GitLab depending on the build status
 * @kind step
 * @param name
 * @param body
 */
def gitlabCommitStatus(name,body) {}

/**
 * Hide from View
 * @kind step
 * @param body
 */
def hideFromView(body) {}

/**
 * Wait for interactive input
 * @kind step
 * @param message
 * @unique
 */
def input(message) {}

/**
 * Checks if running on a Unix-like node
 * @kind step
 */
def isUnix() {}

/**
 * Jira: Add a comment to issue(s)
 * @kind step
 */
def jiraComment() {}

/**
 * Jira: Issue selector
 * @kind step
 */
def jiraIssueSelector() {}

/**
 * Jira: Search issues
 * @kind step
 * @param jql
 */
def jiraSearch(jql) {}

/**
 * Archive JUnit-formatted test results
 * @kind step
 * @param testResults
 */
def junit(testResults) {}

/**
 * Run on an agent matching this label.
 * @kind agent
 */
def label() {}

/**
 * Load a library on the fly
 * @kind step
 * @param identifier
 */
def library(identifier) {}

/**
 * Load a resource file from a library
 * @kind step
 * @param resource
 */
def libraryResource(resource) {}

/**
 * Evaluate a Groovy source file into the Pipeline script
 * @kind step
 * @param path
 */
def load(path) {}

/**
 * Mail
 * @kind step
 */
def mail() {}

/**
 * Run a stage across a matrix of <code>axes</code> combinations.
 * @kind directive
 * @allowedIn stage
 * @unique
 */
def matrix() {}

/**
 * The milestone step forces all builds to go through in order
 * @kind step
 * @param ordinal
 */
def milestone(ordinal) {}

/**
 * Allocate node
 * @kind step
 * @param label
 * @param body
 */
def node(label,body) {}

/**
 * No global agent; each stage must declare its own.
 * @kind agent
 */
def none() {}

/**
 * Pipeline/stage options, e.g. <code>timeout</code>, <code>retry</code>, <code>timestamps</code>, <code>disableConcurrentBuilds</code>.
 * @kind directive
 * @allowedIn pipeline, stage
 * @unique
 */
def options() {}

/**
 * Run stages/branches in parallel.
 * @kind directive
 * @unique
 */
def parallel() {}

/**
 * Execute in parallel
 * @kind step
 * @unique
 */
def parallel() {}

/**
 * Build parameters prompted at run time: <code>string</code>, <code>booleanParam</code>, <code>choice</code>, ….
 * @kind directive
 * @allowedIn pipeline
 * @unique
 */
def parameters() {}

/**
 * Root block of a Declarative Pipeline. Contains <code>agent</code>, <code>stages</code>, and optional sections.
 * @kind section
 */
def pipeline() {}

/**
 * Define a podTemplate to use in the kubernetes plugin
 * @kind step
 * @param body
 */
def podTemplate(body) {}

/**
 * Steps to run after a stage/pipeline based on outcome (<code>always</code>, <code>success</code>, <code>failure</code>, …).
 * @kind directive
 * @allowedIn pipeline, stage
 * @unique
 */
def post() {}

/**
 * Windows PowerShell Script
 * @kind step
 * @param script
 */
def powershell(script) {}

/**
 * Set job properties
 * @kind step
 * @param properties
 */
def properties(properties) {}

/**
 * Publish customized checks to SCM platforms
 * @kind step
 */
def publishChecks() {}

/**
 * Publish HTML reports
 * @kind step
 * @param target
 */
def publishHTML(target) {}

/**
 * Determine current directory
 * @kind step
 */
def pwd() {}

/**
 * PowerShell Core Script
 * @kind step
 * @param script
 */
def pwsh(script) {}

/**
 * Read file from workspace
 * @kind step
 * @param file
 */
def readFile(file) {}

/**
 * Read file from SCM
 * @kind step
 */
def readScmFile() {}

/**
 * Read trusted file from SCM
 * @kind step
 * @param path
 */
def readTrusted(path) {}

/**
 * Post condition: run if the previous run succeeded and this one failed/unstable/aborted.
 * @kind post
 * @allowedIn post
 * @unique
 */
def regression() {}

/**
 * Resolves an SCM from an SCM Source and a list of candidate target branch names
 * @kind step
 */
def resolveScm() {}

/**
 * Retry the body up to N times
 * @kind step
 * @param count
 * @param body
 */
def retry(count,body) {}

/**
 * Escape hatch to run a block of scripted (Groovy) pipeline inside Declarative.
 * @kind directive
 */
def script() {}

/**
 * Run arbitrary Pipeline script
 * @kind step
 * @param body
 */
def script(body) {}

/**
 * Set GitHub PullRequest Commit Status
 * @kind step
 */
def setGitHubPullRequestStatus() {}

/**
 * Shell Script
 * @kind step
 * @param script
 */
def sh(script) {}

/**
 * Send Slack Message
 * @kind step
 */
def slackSend() {}

/**
 * Upload file to slack
 * @kind step
 * @param filePath
 */
def slackUploadFile(filePath) {}

/**
 * Resolve Slack UserId from Email Address
 * @kind step
 * @param email
 */
def slackUserIdFromEmail(email) {}

/**
 * Resolve Slack UserIds from Changeset Authors
 * @kind step
 */
def slackUserIdsFromCommitters() {}

/**
 * Sleep
 * @kind step
 * @param time
 */
def sleep(time) {}

/**
 * SSH Agent
 * @kind step
 * @param credentials
 * @param body
 */
def sshagent(credentials,body) {}

/**
 * A named phase of the pipeline: <code>stage('Build') { steps { … } }</code>.
 * @kind directive
 * @allowedIn stages
 */
def stage() {}

/**
 * Stage
 * @kind step
 * @param name
 * @param body
 * @allowedIn stages
 */
def stage(name,body) {}

/**
 * Container for one or more <code>stage</code> blocks — the work of the pipeline.
 * @kind section
 * @allowedIn pipeline, stage
 * @unique
 */
def stages() {}

/**
 * Stash some files to be used later in the build
 * @kind step
 * @param name
 */
def stash(name) {}

/**
 * General Build Step
 * @kind step
 * @param delegate
 */
def step(delegate) {}

/**
 * The actual commands to run inside a <code>stage</code>.
 * @kind section
 * @allowedIn stage
 * @unique
 */
def steps() {}

/**
 * Post condition: run only if the build succeeded.
 * @kind post
 * @allowedIn post
 * @unique
 */
def success() {}

/**
 * Enforce time limit
 * @kind step
 * @param time
 * @param body
 */
def timeout(time,body) {}

/**
 * Timestamps
 * @kind step
 * @param body
 */
def timestamps(body) {}

/**
 * Expand a string containing macros
 * @kind step
 * @param stringWithMacro
 */
def tm(stringWithMacro) {}

/**
 * Use a tool from a predefined Tool Installation
 * @kind step
 * @param name
 */
def tool(name) {}

/**
 * Auto-install and add tools to PATH, e.g. <code>maven</code>, <code>jdk</code>, <code>gradle</code>.
 * @kind directive
 * @allowedIn pipeline, stage
 * @unique
 */
def tools() {}

/**
 * Automatic triggers: <code>cron</code>, <code>pollSCM</code>, <code>upstream</code>.
 * @kind directive
 * @allowedIn pipeline
 * @unique
 */
def triggers() {}

/**
 * Advanced/Deprecated Copy archived artifacts into the workspace
 * @kind step
 */
def unarchive() {}

/**
 * Post condition: run if the build is unstable.
 * @kind post
 * @allowedIn post
 * @unique
 */
def unstable() {}

/**
 * Set stage result to unstable
 * @kind step
 * @param message
 * @allowedIn post
 * @unique
 */
def unstable(message) {}

/**
 * Restore files previously stashed
 * @kind step
 * @param name
 */
def unstash(name) {}

/**
 * Post condition: run if the build did not succeed.
 * @kind post
 * @allowedIn post
 * @unique
 */
def unsuccessful() {}

/**
 * Update the commit status in GitLab
 * @kind step
 */
def updateGitlabCommitStatus() {}

/**
 * Validate a file containing a Declarative Pipeline
 * @kind step
 * @param path
 */
def validateDeclarativePipeline(path) {}

/**
 * VaultReadStep
 * @kind step
 */
def vault() {}

/**
 * Wait for build to complete
 * @kind step
 * @param runId
 */
def waitForBuild(runId) {}

/**
 * Wait for condition
 * @kind step
 * @param body
 */
def waitUntil(body) {}

/**
 * Catch error and set build and stage result to unstable
 * @kind step
 * @param message
 * @param body
 */
def warnError(message,body) {}

/**
 * Condition that decides whether a stage runs (e.g. <code>branch</code>, <code>expression</code>, <code>allOf</code>).
 * @kind directive
 * @allowedIn stage
 * @unique
 */
def when() {}

/**
 * Inject checks properties into its closure
 * @kind step
 * @param name
 * @param body
 */
def withChecks(name,body) {}

/**
 * Advanced/Deprecated Use contextual object from internal APIs within a block
 * @kind step
 * @param context
 * @param body
 */
def withContext(context,body) {}

/**
 * Bind credentials to variables
 * @kind step
 * @param bindings
 * @param body
 */
def withCredentials(bindings,body) {}

/**
 * Advanced/Deprecated Run build steps inside a Docker container
 * @kind step
 * @param image
 * @param body
 */
def withDockerContainer(image,body) {}

/**
 * Advanced/Deprecated Sets up Docker registry endpoint
 * @kind step
 * @param registry
 * @param body
 */
def withDockerRegistry(registry,body) {}

/**
 * Advanced/Deprecated Sets up Docker server endpoint
 * @kind step
 * @param server
 * @param body
 */
def withDockerServer(server,body) {}

/**
 * Set environment variables
 * @kind step
 * @param overrides
 * @param body
 */
def withEnv(overrides,body) {}

/**
 * Vault Plugin
 * @kind step
 * @param vaultSecrets
 * @param body
 */
def withVault(vaultSecrets,body) {}

/**
 * General Build Wrapper
 * @kind step
 * @param delegate
 * @param body
 */
def wrap(delegate,body) {}

/**
 * Write file to workspace
 * @kind step
 */
def writeFile() {}

/**
 * Allocate workspace
 * @kind step
 * @param dir
 * @param body
 */
def ws(dir,body) {}


// ---- when conditions ----
/**
 * Matches when the nested condition does not match.
 * @kind when
 * @allowedIn when, allOf, anyOf, not
 */
def not() {}

/**
 * Matches when files in the changeset match the given pattern.
 * @kind when
 * @allowedIn when, allOf, anyOf, not
 */
def changeset() {}

/**
 * Matches when the changelog matches the given regular expression.
 * @kind when
 * @allowedIn when, allOf, anyOf, not
 */
def changelog() {}

/**
 * Matches on a change request (pull/merge request).
 * @kind when
 * @allowedIn when, allOf, anyOf, not
 */
def changeRequest() {}

/**
 * Matches when the build is building a tag.
 * @kind when
 * @allowedIn when, allOf, anyOf, not
 */
def buildingTag() {}

/**
 * Matches when building the given tag pattern.
 * @kind when
 * @allowedIn when, allOf, anyOf, not
 */
def tag() {}

/**
 * Matches when the build was triggered by the given cause.
 * @kind when
 * @allowedIn when, allOf, anyOf, not
 */
def triggeredBy() {}

/**
 * Matches when expected equals actual.
 * @kind when
 * @allowedIn when, allOf, anyOf, not
 */
def equals() {}

/**
 * Matches when the run was restarted.
 * @kind when
 * @allowedIn when, allOf, anyOf, not
 */
def isRestartedRun() {}


// ---- options directives ----
/**
 * Disallow concurrent executions of the pipeline.
 * @kind step
 */
def disableConcurrentBuilds() {}

/**
 * Do not allow the pipeline to resume if the controller restarts.
 * @kind step
 */
def disableResume() {}

/**
 * Run each stage in its own container (docker/dockerfile agents).
 * @kind step
 */
def newContainerPerStage() {}

/**
 * Override the default branch-indexing trigger behaviour.
 * @kind step
 */
def overrideIndexTriggers() {}

/**
 * Preserve stashes from the most recent completed builds.
 * @kind step
 */
def preserveStashes() {}

/**
 * Set the quiet period (seconds) for the pipeline.
 * @kind step
 */
def quietPeriod() {}

/**
 * Skip the implicit checkout scm in each stage's agent.
 * @kind step
 */
def skipDefaultCheckout() {}

/**
 * Skip remaining stages once the build is unstable.
 * @kind step
 */
def skipStagesAfterUnstable() {}

/**
 * Make all parallel stages fail fast.
 * @kind step
 */
def parallelsAlwaysFailFast() {}

/**
 * Check out the SCM to a subdirectory of the workspace.
 * @kind step
 */
def checkoutToSubdirectory() {}

/**
 * Set the pipeline durability hint.
 * @kind step
 */
def durabilityHint() {}

/**
 * Limit the rate at which builds are triggered.
 * @kind step
 */
def rateLimitBuilds() {}


// ---- agent / docker / kubernetes configuration ----
/**
 * Docker image to run the agent in.
 * @kind agent
 */
def image() {}

/**
 * URL of the Docker registry.
 * @kind agent
 */
def registryUrl() {}

/**
 * Jenkins credentials ID for the Docker registry.
 * @kind agent
 */
def registryCredentialsId() {}

/**
 * Extra arguments passed to docker run.
 * @kind agent
 */
def args() {}

/**
 * Always pull the image before running.
 * @kind agent
 */
def alwaysPull() {}

/**
 * Run in a custom workspace path.
 * @kind agent
 */
def customWorkspace() {}

/**
 * Reuse the node/workspace of the enclosing stage.
 * @kind agent
 */
def reuseNode() {}

/**
 * Path to the Dockerfile to build the agent image from.
 * @kind agent
 */
def filename() {}

/**
 * Extra arguments passed to docker build.
 * @kind agent
 */
def additionalBuildArgs() {}

/**
 * Inline Kubernetes pod template YAML.
 * @kind agent
 */
def yaml() {}

/**
 * Path to a Kubernetes pod template YAML file.
 * @kind agent
 */
def yamlFile() {}

/**
 * Default container to run steps in (Kubernetes).
 * @kind agent
 */
def defaultContainer() {}

/**
 * Name of the Kubernetes cloud to use.
 * @kind agent
 */
def cloud() {}

/**
 * Kubernetes namespace for the agent pod.
 * @kind agent
 */
def namespace() {}

/**
 * Kubernetes service account for the agent pod.
 * @kind agent
 */
def serviceAccount() {}


// ---- parameters / triggers / tools calls ----
/**
 * A string build parameter.
 * @kind step
 */
def string() {}

/**
 * A multi-line text build parameter.
 * @kind step
 */
def text() {}

/**
 * A boolean build parameter.
 * @kind step
 */
def booleanParam() {}

/**
 * A choice (drop-down) build parameter.
 * @kind step
 */
def choice() {}

/**
 * A password build parameter.
 * @kind step
 */
def password() {}

/**
 * A file build parameter.
 * @kind step
 */
def file() {}

/**
 * A credentials build parameter.
 * @kind step
 */
def credentials() {}

/**
 * Trigger the pipeline on a cron schedule.
 * @kind step
 */
def cron() {}

/**
 * Poll the SCM on a schedule and build on change.
 * @kind step
 */
def pollSCM() {}

/**
 * Trigger after the given upstream jobs complete.
 * @kind step
 */
def upstream() {}

/**
 * Use the named Maven installation.
 * @kind step
 */
def maven() {}

/**
 * Use the named JDK installation.
 * @kind step
 */
def jdk() {}

/**
 * Use the named Gradle installation.
 * @kind step
 */
def gradle() {}

/**
 * Use the named NodeJS installation.
 * @kind step
 */
def nodejs() {}

/**
 * Use the named Ant installation.
 * @kind step
 */
def ant() {}


// ---- agent types ----
/**
 * Run the pipeline/stage in a container built from a Dockerfile in the source repository.
 * @kind agent
 */
def dockerfile() {}

/**
 * Run the pipeline/stage in a pod on a Kubernetes cluster.
 * @kind agent
 */
def kubernetes() {}


// ---- matrix + remaining options ----
/**
 * Keep only recent build history/artifacts (e.g. logRotator(numToKeepStr: '10')).
 * @kind step
 */
def buildDiscarder() {}

/**
 * Disallow restarting the pipeline from a stage.
 * @kind step
 */
def disableRestartFromStage() {}

/**
 * The values for a matrix axis.
 * @kind step
 * @allowedIn axis
 */
def values() {}

/**
 * Container for the matrix axes.
 * @kind directive
 * @allowedIn matrix
 * @unique
 */
def axes() {}

/**
 * One axis of a matrix: a name and its values.
 * @kind directive
 * @allowedIn axes, exclude
 */
def axis() {}

/**
 * Combinations to exclude from the matrix.
 * @kind directive
 * @allowedIn matrix
 * @unique
 */
def excludes() {}

/**
 * One excluded combination of axis values.
 * @kind directive
 * @allowedIn excludes
 */
def exclude() {}

